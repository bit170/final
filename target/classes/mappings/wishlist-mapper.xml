<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="wishlistDAO">
	
	<!-- <insert id="insertWishlist" parameterType="wishlist">
		INSERT INTO WISHLIST (P_CODE, ID)
		VALUES #{p_code}, #{id}
	</insert>
	
	<update id="updateWishlist" parameterType="wishlist">
		UPDATE WISHLIST
		SET P_CODE = #{p_code}, ID = #{id}
		WHERE ID = #{id}
	</update>
	
	<delete id="deleteWishlist" parameterType="wishlist">
		DELETE WISHLIST WHERE O_CODE = #{o_code}
	</delete>
	
	<delete id="removeWishlist" parameterType="wishlist">
		DELETE WISHLIST WHERE ID = #{id}
	</delete>
	
	<select id="getWishlist" parameterType="wishlist">
		SELECT * FROM WISHLIST WHERE O_CODE = #{O_CODE}
	</select>
	
	<select id="getWishlistList" parameterType="wishlist">
		SELECT * FROM WISHLIST
	</select> -->
	
</mapper>