<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="followDAO">
	
	<!-- <insert id="insertFollow" parameterType="follow">
		INSERT INTO FOLLOW (ID, NICKNAME)
		VALUES #{id}, #{nickname}
	</insert>
	
	<update id="updateFollow" parameterType="follow">
		UPDATE FOLLOW
		SET ID = #{id}, NICKNAME = #{nickname}
		WHERE ID = #{id}
	</update>
	
	<delete id="deleteFollow" parameterType="follow">
		DELETE FOLLOW WHERE ID = #{id}
	</delete>
	
	<select id="getFollow" parameterType="follow">
		SELECT * FROM FOLLOW WHERE ID = #{id}
	</select>
	
	<select id="getFollowList" parameterType="follow">
		SELECT * FROM FOLLOW
		WHERE NICKNAME = #{nickname}
	</select> -->
	
</mapper>