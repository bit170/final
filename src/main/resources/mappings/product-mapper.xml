<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="productDAO">

	<resultMap id="ProductList" type="pvo"/>
	<select id="getProductList" parameterType="pvo" resultMap="ProductList">
		SELECT * FROM PRODUCT
	</select>
	
	<!-- 맞는 쿼리문 작성할 것(메인에 출력할 작품리스트 -->
	<select id="getMainProduct" resultMap="ProductList">
		select * from product
	</select>
	
	<select id="getProduct" parameterType="pvo" resultType="pvo">
		SELECT * FROM PRODUCT WHERE P_CODE=#{p_code}
	</select>
	
</mapper>